[pytest]
addopts = -ra --strict-config --strict-markers
junit_family = legacy
console_output_style = progress
cache_dir = /tmp/hermes-controller-e2e-pytest-cache
markers =
    case(test_case_id, requirement_ids, critical, assumptions=()): approved case metadata
